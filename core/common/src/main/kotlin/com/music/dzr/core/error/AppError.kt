package com.music.dzr.core.error

/**
 * Marker interface for every domain-specific error that can be returned
 * from an operation and wrapped into a [com.music.dzr.core.result.Result].
 */
interface AppError
